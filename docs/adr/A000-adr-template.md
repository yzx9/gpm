# ANNN: <Title>

**Date:** YYYY-MM-DD · **Status:** Accepted | Superseded by ANNN | Deprecated

<!--
An ADR records a foundational, cross-cutting, hard-to-reverse decision — "why
this, at architecture level, over the alternatives." It is NOT a feature spec
(docs/specs/) or a design RFC (docs/rfcs/). See AGENTS.md "When to write an
ADR" for the bar.

Frozen once Accepted; if a decision is reversed, mark this file Superseded and
write a new ADR — never delete or rewrite a past one.

Naming: `ANNN-kebab-title.md`, 3-digit zero-padded; next number = current max + 1
across all branches. Delete this comment when filling the ADR in.
-->

## Context

The problem and the forces at play — constraints, prior decisions, what's
unsatisfying about the current state. Cross-cutting (it shapes more than one
feature), and decision-altitude only; no code.

## Decision

The choice, stated in one or two plain sentences.

## Consequences

What follows: what gets easier, what gets harder or ruled out, the trade-offs
accepted. Flag anything a future reader must honor.

## Alternatives considered

The serious options and why each was rejected — the audit trail, kept brief.
